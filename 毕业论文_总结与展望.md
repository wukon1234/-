# 毕业设计总结与展望（商城智能助手系统）

## 一、项目总结

本次毕业设计围绕“商城智能助手系统”展开，目标并不是单纯搭建一个商品展示网站，而是尝试把传统电商流程和大模型能力融合起来，做一个同时具备交易闭环与智能服务能力的系统。从项目结果看，系统已经形成了前后端分离的完整架构：前端基于 Vue3 + TypeScript + Element Plus，后端基于 Spring Boot + MyBatis Plus，并通过 MySQL、Redis 以及可扩展的向量检索能力构成业务与智能能力的基础支撑。  

在功能实现层面，系统覆盖了用户、商品、购物车、订单、地址、收藏、评价等核心商城模块，同时额外实现了智能助手、内容审核、管理端运营分析、AI 生成文案等差异化能力。以用户链路为例，系统支持用户注册登录、管理员登录、用户信息获取与密码修改，后端通过 JWT 拦截器统一处理鉴权逻辑，并将 `userId` 注入请求上下文，业务接口可直接基于用户身份执行权限校验。密码采用 BCrypt 加密存储，避免明文密码带来的安全隐患。  

订单模块是本项目中工程性较强的一部分。系统在创建订单时不仅进行金额汇总，还会对商品状态和库存进行严格校验：商品下架不可下单、库存不足会直接拦截。下单成功后会写入订单主表和订单项表，按数量扣减库存，并删除已结算购物车项，整个流程置于事务中保证一致性。用户取消订单会触发库存回补，支付接口会更新支付时间和状态字段，这些处理虽然是“模拟支付”场景，但业务状态流转已经具备电商系统的基本规范。  

管理端能力方面，项目并没有停留在“增删改查”层面，而是补充了可用于运营分析的接口。后台仪表盘可以统计商品总量、订单总量、用户总量、月新增数据、近单数据与热销商品；订单分析接口支持按时间、商品、用户维度生成图表基础数据，并通过大模型接口生成“结论-问题-建议”的运营分析文本。这部分设计体现了本课题“智能化运营辅助”的方向，使管理后台不只是信息展示页面，而是具备了一定决策支持能力。  

最有代表性的创新模块是智能助手。系统实现了完整的对话生命周期管理：自动创建会话、保存用户消息与助手回复、按会话分页查询历史、删除会话记录、提取关联商品信息。智能助手并非直接把用户问题扔给大模型，而是采用“检索增强 + 提示词约束”的方式构建回答链路：先进行商品检索，再把检索到的商品信息拼接进提示词，要求模型围绕商城场景作答，并在无关问题时进行礼貌拒答，尽量降低泛化回答带来的跑题风险。  

在交互体验上，系统实现了 SSE 流式对话接口，支持逐段推送内容，且在服务端做了语义单元切分，中文按字、英文按词输出，减少英文词被截断造成的阅读割裂感。对于代理缓冲导致的流式延迟问题，接口中增加了 `X-Accel-Buffering: no` 等响应头，保证消息尽快到达前端。此外，助手在流式结束后还会附带相关推荐商品，提升问答到交易转化的衔接效率。  

RAG 相关能力采用了“可降级架构”。在向量服务可用且配置开启时，系统使用向量检索；若向量检索结果为空或依赖异常，则自动回退到关键词检索，保证服务可用性。向量数据同步支持批量初始化与单商品增量更新，商品文本会融合名称、描述、规格、价格、库存等信息，用于提升召回质量。这种设计在毕设环境下具有现实意义：既能体现智能检索能力，又能兼顾资源受限场景下的稳定运行。  

内容安全方面，项目增加了内容检测拦截链路。通过请求体缓存过滤器解决“请求体只能读取一次”的问题，再由内容检测拦截器对聊天、评论、公告等入口进行统一拦截，违规内容会在进入业务逻辑前被拒绝，避免不当内容直接进入模型或数据库。虽然规则和模型能力还有优化空间，但这一套机制已经体现了“功能可用之外，系统需考虑风险控制”的工程意识。  

前端实现上，项目完成了用户端与管理端的主要页面结构，典型如管理员用户列表页面，支持关键词、状态、角色筛选，具备分页、状态开关、角色切换、弹窗编辑等功能；接口层通过统一 request 封装管理 API 调用，前端状态和交互反馈较完整。整体而言，界面不是“静态演示”，而是围绕真实接口形成了可联调、可演示、可扩展的应用形态。  

从毕设目标达成度来看，本项目已经完成了“基础电商业务 + 智能助手增强 + 管理端运营支持”的三层建设，既有完整业务闭环，也有一定智能化深度，具备论文所需的系统性与实践价值。

## 二、项目不足与反思

虽然系统功能覆盖较全，但从工程成熟度来看仍有不少可优化空间。首先是权限体系目前以角色字段和接口约束为主，缺少更细粒度的 RBAC 权限模型，例如菜单级、按钮级、数据范围级控制。对于毕业设计演示而言已够用，但若面向真实多岗位运营团队，权限治理仍然不够精细。  

其次，在性能与并发方面，当前订单扣减库存逻辑主要依赖事务一致性，尚未引入更强的并发控制策略（如乐观锁版本号、分布式锁、消息削峰等）。在高并发秒杀类场景下，现有方案可能会面临库存超卖风险。  

再次，智能助手虽然实现了 RAG 与流式输出，但知识边界仍主要围绕商品与商城流程，尚未建立更系统的知识库管理机制，例如文档分片策略、检索评估指标、召回重排策略、提示词版本管理等。目前的实现更偏“功能打通”，距离“可持续优化的智能中台”还有距离。  

此外，前端与后端在少量细节上还存在阶段性差异，例如个别页面分页总数采用列表长度回填，和后端真实分页总数字段还可进一步对齐；部分异常提示信息仍较通用，尚可增加更精准的错误码映射与用户引导。测试方面，目前以接口联调和功能验证为主，自动化测试（单元测试、集成测试、端到端测试）覆盖率偏低，这也是后续工程化提升的重点。  

最后，部署与运维部分还处于基础阶段，虽然本地启动链路完整，但缺少标准化的容器部署、日志聚合、监控告警、灰度发布机制。对于毕设答辩来说可通过演示覆盖需求，但若进入长期迭代，运维能力必须同步建设。

## 三、未来展望

结合当前系统现状，后续优化可以从“业务深化、智能增强、工程治理”三个方向推进。

第一，业务深化方面，可以补齐更完整的电商链路能力：接入真实支付回调与订单状态机、完善物流跟踪节点、支持优惠券与满减规则引擎、增加售后工单与退款流程。这样可以让系统从“教学型商城”逐步走向“准生产级业务系统”。  

第二，智能增强方面，建议把助手能力从“问答助手”扩展为“交易助手 + 运营助手”。对用户侧，可做个性化推荐、相似商品对比、购物决策解释、售后智能分流；对管理侧，可做商品文案自动生成优化、评论情感分析、异常订单识别、自动运营周报生成。技术上可以引入多模型路由、意图识别、工具调用（如查库存、查订单）等机制，让模型从“会说”走向“会做”。  

第三，RAG 能力建议继续体系化。可以在数据层引入结构化知识与非结构化知识的双通道检索，在召回层增加混合检索（关键词 + 向量），在排序层加入重排模型，在评估层建立命中率、相关性、响应时延等指标，形成可量化优化闭环。这样论文中也可进一步增加“实验对比”内容，比如关键词检索与混合检索在不同问题类型下的效果差异。  

第四，工程治理方面，建议补齐 CI/CD 与质量门禁流程：提交触发单元测试、静态检查、接口契约校验；发布前执行自动化回归；上线后接入指标监控和错误告警。对于后端可增加统一日志追踪 ID、慢查询监控、接口限流；前端可增加埋点分析和性能监测。只有把这些基础能力补齐，系统才能从“可演示”进化到“可长期维护”。  

第五，安全与合规层面，后续可以引入更完整的策略，如敏感词分级、图片与文本多模态审核、用户行为风控、管理操作审计日志、数据脱敏展示与隐私保护机制。尤其在接入大模型后，提示注入、越权回答、幻觉信息等问题需要配套防护机制，例如系统提示词加固、上下文白名单、高风险回答兜底模板等。  

第六，从论文研究价值角度，还可以把系统演进过程提炼为可复用方法论：  
一是“传统业务系统智能化改造路径”，即先打通核心交易链路，再引入智能模块并通过拦截器与中间层实现低侵入接入；  
二是“稳定性优先的智能架构策略”，即在智能能力链路中默认提供降级与回退，避免单点能力失效导致整体不可用；  
三是“业务价值导向的 AI 落地原则”，即智能功能必须服务转化率、客服效率、运营决策等可观察指标，而不是只追求技术新颖。  

总体来说，这个毕业设计的价值不在于追求“大而全”，而在于完整走通了一个智能电商系统从需求分析、架构设计、模块实现到风险控制的全过程。项目已经具备较好的扩展基础，后续只要沿着工程化与产品化方向持续迭代，就能够从毕业设计原型逐步成长为具有实际应用潜力的系统。
