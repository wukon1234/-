# 第五章 系统实现

本章详细介绍商城智能购物助手系统的具体实现过程，包括开发环境配置、核心模块的代码实现、关键技术的应用以及系统界面展示等内容。

## 5.1 开发环境配置

### 5.1.1 后端环境配置

**（1）项目依赖配置**

后端项目使用Maven进行依赖管理，核心依赖配置如下：

```xml
<dependencies>
    <!-- Spring Boot Web -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    
    <!-- MyBatis Plus -->
    <dependency>
        <groupId>com.baomidou</groupId>
        <artifactId>mybatis-plus-boot-starter</artifactId>
        <version>3.5.3</version>
    </dependency>
    
    <!-- MySQL Driver -->
    <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
        <scope>runtime</scope>
    </dependency>
    
    <!-- JWT -->
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt</artifactId>
        <version>0.9.1</version>
    </dependency>
    
    <!-- Lombok -->
    <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <optional>true</optional>
    </dependency>
</dependencies>
```

**（2）应用配置文件**

application.yml配置文件包含数据库连接、JWT密钥、LLM服务等配置：

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mall_assistant
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: your-secret-key
  expiration: 86400000

llm:
  provider: qwen
  api-key: your-api-key
```

### 5.1.2 前端环境配置

**（1）项目初始化**

前端项目使用Vite创建，技术栈包括Vue 3、TypeScript、Element Plus：

```bash
npm create vite@latest mall-frontend -- --template vue-ts
cd mall-frontend
npm install
npm install element-plus pinia axios vue-router
```

前端开发环境建议使用Node.js 16+，并保持npm或pnpm版本与项目依赖一致，以避免包管理和构建时的版本冲突。

本系统前端还建议在团队开发过程中使用统一的代码风格和提交规范，如ESLint、Prettier与Git commit hooks，以保证代码质量和可维护性。

**（2）Vite配置**

vite.config.ts配置文件包含开发服务器代理等配置：

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  },
  resolve: {
    alias: {
      '@': '/src'
    }
  }
})
```

在实际部署前，建议通过环境变量区分开发、测试和生产环境。前端可使用`.env.development`和`.env.production`分别配置API地址、日志级别和第三方服务开关。这样当系统上线时，只需切换构建环境即可保持正确的服务指向，避免硬编码带来的风险。

### 5.1.3 数据库初始化

执行数据库初始化脚本创建表结构和初始数据：

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS mall_assistant 
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE mall_assistant;

-- 创建用户表
CREATE TABLE user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    email VARCHAR(100),
    phone VARCHAR(20),
    avatar VARCHAR(255),
    role TINYINT DEFAULT 0,
    status TINYINT DEFAULT 1,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME ON UPDATE CURRENT_TIMESTAMP
);

-- 创建商品表
CREATE TABLE product (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(200) NOT NULL,
    description TEXT,
    category_id BIGINT,
    price DECIMAL(10,2) NOT NULL,
    stock INT DEFAULT 0,
    image_url VARCHAR(500),
    status TINYINT DEFAULT 1,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME ON UPDATE CURRENT_TIMESTAMP
);

-- 其他表结构省略...
```

## 5.2 用户认证模块实现

### 5.2.1 JWT工具类实现

JwtUtil工具类负责Token的生成和解析：

```java
@Component
public class JwtUtil {
    
    @Value("${jwt.secret}")
    private String secret;
    
    @Value("${jwt.expiration}")
    private Long expiration;
    
    /**
     * 生成JWT Token
     */
    public String generateToken(Long userId) {
        Date now = new Date();
        Date expiryDate = new Date(now.getTime() + expiration);
        
        return Jwts.builder()
                .setSubject(String.valueOf(userId))
                .setIssuedAt(now)
                .setExpiration(expiryDate)
                .signWith(SignatureAlgorithm.HS512, secret)
                .compact();
    }
    
    /**
     * 从Token中获取用户ID
     */
    public Long getUserIdFromToken(String token) {
        Claims claims = Jwts.parser()
                .setSigningKey(secret)
                .parseClaimsJws(token)
                .getBody();
        return Long.parseLong(claims.getSubject());
    }
    
    /**
     * 验证Token是否有效
     */
    public boolean validateToken(String token) {
        try {
            Jwts.parser().setSigningKey(secret).parseClaimsJws(token);
            return true;
        } catch (Exception e) {
            return false;
        }
    }
}
```

### 5.2.2 JWT拦截器实现

JwtInterceptor拦截器验证请求中的Token：

```java
@Component
public class JwtInterceptor implements HandlerInterceptor {
    
    @Autowired
    private JwtUtil jwtUtil;
    
    @Override
    public boolean preHandle(HttpServletRequest request, 
                            HttpServletResponse response, 
                            Object handler) throws Exception {
        // 放行OPTIONS请求
        if ("OPTIONS".equals(request.getMethod())) {
            return true;
        }
        
        // 获取Authorization头
        String authHeader = request.getHeader("Authorization");
        if (authHeader == null || !authHeader.startsWith("Bearer ")) {
            response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);
            response.getWriter().write("{\"code\":401,\"message\":\"未登录\"}");
            return false;
        }
        
        // 提取并验证Token
        String token = authHeader.substring(7);
        if (!jwtUtil.validateToken(token)) {
            response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);
            response.getWriter().write("{\"code\":401,\"message\":\"Token无效\"}");
            return false;
        }
        
        // 将用户ID存入请求属性
        Long userId = jwtUtil.getUserIdFromToken(token);
        request.setAttribute("userId", userId);
        return true;
    }
}
```

### 5.2.3 用户服务实现

UserServiceImpl实现用户注册和登录逻辑：

```java
@Service
public class UserServiceImpl extends ServiceImpl<UserMapper, User> 
        implements UserService {
    
    @Autowired
    private JwtUtil jwtUtil;
    
    @Autowired
    private BCryptPasswordEncoder passwordEncoder;
    
    @Override
    public Result register(String username, String password) {
        // 检查用户名是否已存在
        User existUser = this.getOne(
            new LambdaQueryWrapper<User>()
                .eq(User::getUsername, username)
        );
        if (existUser != null) {
            return Result.error("用户名已存在");
        }
        
        // 创建新用户
        User user = new User();
        user.setUsername(username);
        user.setPassword(passwordEncoder.encode(password));
        user.setRole(0);
        user.setStatus(1);
        this.save(user);
        
        return Result.success("注册成功");
    }
    
    @Override
    public Result login(String username, String password) {
        // 查询用户
        User user = this.getOne(
            new LambdaQueryWrapper<User>()
                .eq(User::getUsername, username)
        );
        if (user == null) {
            return Result.error("用户不存在");
        }
        
        // 验证密码
        if (!passwordEncoder.matches(password, user.getPassword())) {
            return Result.error("密码错误");
        }
        
        // 生成Token
        String token = jwtUtil.generateToken(user.getId());
        
        // 构建返回数据
        Map<String, Object> data = new HashMap<>();
        data.put("token", token);
        data.put("userInfo", user);
        
        return Result.success(data);
    }
}
```

## 5.3 商品模块实现

### 5.3.1 商品列表查询

ProductController提供商品列表接口：

```java
@RestController
@RequestMapping("/api/products")
public class ProductController {
    
    @Autowired
    private ProductService productService;
    
    /**
     * 分页查询商品列表
     */
    @GetMapping
    public Result list(
            @RequestParam(defaultValue = "1") Integer page,
            @RequestParam(defaultValue = "10") Integer size,
            @RequestParam(required = false) Long categoryId,
            @RequestParam(required = false) String keyword) {
        
        Page<Product> pageParam = new Page<>(page, size);
        LambdaQueryWrapper<Product> wrapper = new LambdaQueryWrapper<>();
        
        // 只查询上架商品
        wrapper.eq(Product::getStatus, 1);
        
        // 分类筛选
        if (categoryId != null) {
            wrapper.eq(Product::getCategoryId, categoryId);
        }
        
        // 关键词搜索
        if (keyword != null && !keyword.isEmpty()) {
            wrapper.like(Product::getName, keyword);
        }
        
        // 按创建时间倒序
        wrapper.orderByDesc(Product::getCreateTime);
        
        Page<Product> result = productService.page(pageParam, wrapper);
        return Result.success(result);
    }
    
    /**
     * 获取商品详情
     */
    @GetMapping("/{id}")
    public Result detail(@PathVariable Long id) {
        Product product = productService.getById(id);
        if (product == null) {
            return Result.error("商品不存在");
        }
        return Result.success(product);
    }
}
```

### 5.3.2 商品搜索实现

商品搜索支持模糊匹配和分类筛选：

```java
@Service
public class ProductServiceImpl extends ServiceImpl<ProductMapper, Product>
        implements ProductService {
    
    @Override
    public List<Product> searchProducts(String keyword, int limit) {
        LambdaQueryWrapper<Product> wrapper = new LambdaQueryWrapper<>();
        wrapper.eq(Product::getStatus, 1)
               .and(w -> w.like(Product::getName, keyword)
                         .or()
                         .like(Product::getDescription, keyword))
               .last("LIMIT " + limit);
        return this.list(wrapper);
    }
    
    @Override
    public List<Product> getByCategory(Long categoryId) {
        return this.list(
            new LambdaQueryWrapper<Product>()
                .eq(Product::getCategoryId, categoryId)
                .eq(Product::getStatus, 1)
        );
    }
}
```

## 5.4 购物车模块实现

### 5.4.1 添加购物车

CartController处理购物车操作：

```java
@RestController
@RequestMapping("/api/cart")
public class CartController {
    
    @Autowired
    private CartService cartService;
    
    /**
     * 添加商品到购物车
     */
    @PostMapping("/add")
    public Result add(@RequestBody CartAddRequest request,
                     HttpServletRequest httpRequest) {
        Long userId = (Long) httpRequest.getAttribute("userId");
        return cartService.addToCart(userId, request.getProductId(), 
                                     request.getQuantity());
    }
    
    /**
     * 获取购物车列表
     */
    @GetMapping("/list")
    public Result list(HttpServletRequest request) {
        Long userId = (Long) request.getAttribute("userId");
        List<CartVO> cartList = cartService.getCartList(userId);
        return Result.success(cartList);
    }
}
```

### 5.4.2 购物车服务实现

CartServiceImpl实现购物车业务逻辑：

```java
@Service
public class CartServiceImpl extends ServiceImpl<CartMapper, Cart>
        implements CartService {
    
    @Autowired
    private ProductService productService;
    
    @Override
    @Transactional
    public Result addToCart(Long userId, Long productId, Integer quantity) {
        // 检查商品是否存在
        Product product = productService.getById(productId);
        if (product == null || product.getStatus() != 1) {
            return Result.error("商品不存在或已下架");
        }
        
        // 检查库存
        if (product.getStock() < quantity) {
            return Result.error("库存不足");
        }
        
        // 检查是否已在购物车中
        Cart existCart = this.getOne(
            new LambdaQueryWrapper<Cart>()
                .eq(Cart::getUserId, userId)
                .eq(Cart::getProductId, productId)
        );
        
        if (existCart != null) {
            // 已存在则更新数量
            int newQuantity = existCart.getQuantity() + quantity;
            if (newQuantity > product.getStock()) {
                return Result.error("库存不足");
            }
            existCart.setQuantity(newQuantity);
            this.updateById(existCart);
        } else {
            // 不存在则新增
            Cart cart = new Cart();
            cart.setUserId(userId);
            cart.setProductId(productId);
            cart.setQuantity(quantity);
            this.save(cart);
        }
        
        return Result.success("添加成功");
    }
    
    @Override
    public List<CartVO> getCartList(Long userId) {
        return baseMapper.getCartListWithProduct(userId);
    }
}
```

## 5.5 订单模块实现

### 5.5.1 创建订单

OrderController处理订单操作：

```java
@RestController
@RequestMapping("/api/order")
public class OrderController {
    
    @Autowired
    private OrderService orderService;
    
    /**
     * 创建订单
     */
    @PostMapping("/create")
    public Result create(@RequestBody OrderCreateRequest request,
                        HttpServletRequest httpRequest) {
        Long userId = (Long) httpRequest.getAttribute("userId");
        return orderService.createOrder(userId, request.getAddressId(), 
                                        request.getCartIds());
    }
    
    /**
     * 支付订单
     */
    @PutMapping("/{id}/pay")
    public Result pay(@PathVariable Long id, HttpServletRequest request) {
        Long userId = (Long) request.getAttribute("userId");
        return orderService.payOrder(userId, id);
    }
}
```

### 5.5.2 订单服务实现

OrderServiceImpl实现订单创建的完整流程：

```java
@Service
public class OrderServiceImpl extends ServiceImpl<OrderMapper, Order>
        implements OrderService {
    
    @Autowired
    private CartService cartService;
    
    @Autowired
    private ProductService productService;
    
    @Autowired
    private OrderItemMapper orderItemMapper;
    
    @Override
    @Transactional(rollbackFor = Exception.class)
    public Result createOrder(Long userId, Long addressId, 
                              List<Long> cartIds) {
        // 1. 查询购物车商品
        List<Cart> cartList = cartService.listByIds(cartIds);
        if (cartList.isEmpty()) {
            return Result.error("购物车为空");
        }
        
        // 2. 验证库存并计算总金额
        BigDecimal totalAmount = BigDecimal.ZERO;
        List<OrderItem> orderItems = new ArrayList<>();
        
        for (Cart cart : cartList) {
            Product product = productService.getById(cart.getProductId());
            if (product.getStock() < cart.getQuantity()) {
                return Result.error(product.getName() + " 库存不足");
            }
            
            BigDecimal itemTotal = product.getPrice()
                .multiply(new BigDecimal(cart.getQuantity()));
            totalAmount = totalAmount.add(itemTotal);
            
            OrderItem item = new OrderItem();
            item.setProductId(product.getId());
            item.setProductName(product.getName());
            item.setQuantity(cart.getQuantity());
            item.setPrice(product.getPrice());
            orderItems.add(item);
        }
        
        // 3. 创建订单
        Order order = new Order();
        order.setOrderNo(generateOrderNo());
        order.setUserId(userId);
        order.setTotalAmount(totalAmount);
        order.setAddressId(addressId);
        order.setStatus(0); // 待支付
        this.save(order);
        
        // 4. 创建订单项
        for (OrderItem item : orderItems) {
            item.setOrderId(order.getId());
            orderItemMapper.insert(item);
        }
        
        // 5. 扣减库存
        for (Cart cart : cartList) {
            productService.decreaseStock(cart.getProductId(), 
                                        cart.getQuantity());
        }
        
        // 6. 删除购物车
        cartService.removeByIds(cartIds);
        
        return Result.success(order);
    }
    
    private String generateOrderNo() {
        return "ORD" + System.currentTimeMillis() + 
               String.format("%04d", new Random().nextInt(10000));
    }
}
```

## 5.6 智能助手模块实现

### 5.6.1 RAG检索服务

RAGService接口定义检索增强生成的能力：

```java
public interface RAGService {
    /**
     * 根据查询检索相关商品
     */
    List<Product> searchRelevantProducts(String query, int topK);
    
    /**
     * 格式化商品信息为上下文
     */
    String formatProductsAsContext(List<Product> products);
}
```

SimpleRAGServiceImpl实现基于关键词的检索：

```java
@Service
public class SimpleRAGServiceImpl implements RAGService {
    
    @Autowired
    private ProductService productService;
    
    @Override
    public List<Product> searchRelevantProducts(String query, int topK) {
        // 提取关键词
        List<String> keywords = extractKeywords(query);
        
        // 搜索商品
        LambdaQueryWrapper<Product> wrapper = new LambdaQueryWrapper<>();
        wrapper.eq(Product::getStatus, 1);
        
        if (!keywords.isEmpty()) {
            wrapper.and(w -> {
                for (String keyword : keywords) {
                    w.or().like(Product::getName, keyword)
                          .or().like(Product::getDescription, keyword);
                }
            });
        }
        
        wrapper.last("LIMIT " + topK);
        return productService.list(wrapper);
    }
    
    @Override
    public String formatProductsAsContext(List<Product> products) {
        if (products.isEmpty()) {
            return "当前没有找到相关商品。";
        }
        
        StringBuilder sb = new StringBuilder();
        sb.append("以下是相关商品信息：\n");
        
        for (int i = 0; i < products.size(); i++) {
            Product p = products.get(i);
            sb.append(String.format("%d. %s - ¥%.2f - %s\n", 
                i + 1, p.getName(), p.getPrice(), 
                p.getDescription().substring(0, 
                    Math.min(50, p.getDescription().length()))));
        }
        
        return sb.toString();
    }
    
    private List<String> extractKeywords(String query) {
        // 简单的关键词提取
        String[] stopWords = {"我", "想", "要", "买", "个", "一", "的"};
        List<String> keywords = new ArrayList<>();
        
        for (String word : query.split("")) {
            if (word.length() >= 2 && 
                !Arrays.asList(stopWords).contains(word)) {
                keywords.add(word);
            }
        }
        
        return keywords;
    }
}
```

### 5.6.2 LLM服务接口

LLMService定义大语言模型调用的抽象接口：

```java
public interface LLMService {
    /**
     * 普通对话（一次性返回）
     */
    String chat(List<Message> messages);
    
    /**
     * 流式对话
     */
    void chatStream(List<Message> messages, StreamCallback callback);
}

@FunctionalInterface
public interface StreamCallback {
    void onToken(String token);
}
```

### 5.6.3 通义千问服务实现

QwenLLMServiceImpl实现通义千问API的调用：

```java
@Service
@ConditionalOnProperty(name = "llm.provider", havingValue = "qwen")
public class QwenLLMServiceImpl implements LLMService {
    
    @Value("${llm.api-key}")
    private String apiKey;
    
    private static final String API_URL = 
        "https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation";
    
    @Override
    public void chatStream(List<Message> messages, StreamCallback callback) {
        try {
            // 构建请求
            HttpRequest request = HttpRequest.newBuilder()
                .uri(URI.create(API_URL))
                .header("Authorization", "Bearer " + apiKey)
                .header("Content-Type", "application/json")
                .header("X-DashScope-SSE", "enable")
                .POST(HttpRequest.BodyPublishers.ofString(
                    buildRequestBody(messages)))
                .build();
            
            // 发送流式请求
            HttpClient client = HttpClient.newHttpClient();
            client.send(request, HttpResponse.BodyHandlers.ofLines())
                .body()
                .forEach(line -> {
                    if (line.startsWith("data:")) {
                        String data = line.substring(5).trim();
                        String token = extractToken(data);
                        if (token != null) {
                            callback.onToken(token);
                        }
                    }
                });
                
        } catch (Exception e) {
            throw new RuntimeException("LLM调用失败", e);
        }
    }
    
    private String buildRequestBody(List<Message> messages) {
        // 构建符合通义千问API格式的请求体
        Map<String, Object> body = new HashMap<>();
        body.put("model", "qwen-turbo");
        body.put("input", Map.of("messages", messages));
        body.put("parameters", Map.of("incremental_output", true));
        
        return new Gson().toJson(body);
    }
}
```

### 5.6.4 智能助手控制器

AssistantController处理智能助手的请求：

```java
@RestController
@RequestMapping("/api/assistant")
public class AssistantController {
    
    @Autowired
    private AssistantService assistantService;
    
    /**
     * SSE流式对话
     */
    @PostMapping("/chat/stream")
    public SseEmitter chatStream(@RequestBody ChatRequest request,
                                 HttpServletRequest httpRequest) {
        Long userId = (Long) httpRequest.getAttribute("userId");
        
        SseEmitter emitter = new SseEmitter(60000L);
        
        // 异步处理
        CompletableFuture.runAsync(() -> {
            try {
                assistantService.handleStreamChat(
                    userId, 
                    request.getConversationId(),
                    request.getMessage(),
                    emitter
                );
            } catch (Exception e) {
                emitter.completeWithError(e);
            }
        });
        
        return emitter;
    }
    
    /**
     * 获取会话列表
     */
    @GetMapping("/conversations")
    public Result getConversations(HttpServletRequest request) {
        Long userId = (Long) request.getAttribute("userId");
        List<Conversation> list = assistantService.getConversations(userId);
        return Result.success(list);
    }
}
```

### 5.6.5 智能助手服务实现

AssistantServiceImpl实现完整的对话处理流程：

```java
@Service
public class AssistantServiceImpl implements AssistantService {
    
    @Autowired
    private RAGService ragService;
    
    @Autowired
    private LLMService llmService;
    
    @Autowired
    private ConversationService conversationService;
    
    private static final String SYSTEM_PROMPT = """
        你是一个专业的购物助手，帮助用户挑选商品和解答购物问题。
        请根据用户的需求和提供的商品信息，给出专业的购物建议。
        回复要简洁友好，如有推荐商品请说明推荐理由。
        """;
    
    @Override
    public void handleStreamChat(Long userId, Long conversationId,
                                 String userMessage, SseEmitter emitter) {
        try {
            // 1. 保存用户消息
            conversationService.saveMessage(conversationId, 
                MessageRole.USER, userMessage);
            
            // 2. RAG检索相关商品
            List<Product> products = ragService
                .searchRelevantProducts(userMessage, 5);
            String context = ragService.formatProductsAsContext(products);
            
            // 3. 获取历史消息
            List<Message> history = conversationService
                .getRecentMessages(conversationId, 10);
            
            // 4. 构建消息列表
            List<Message> messages = new ArrayList<>();
            messages.add(new Message("system", SYSTEM_PROMPT));
            messages.addAll(history);
            messages.add(new Message("user", 
                userMessage + "\n\n参考商品信息：\n" + context));
            
            // 5. 流式调用LLM
            StringBuilder fullResponse = new StringBuilder();
            llmService.chatStream(messages, token -> {
                try {
                    fullResponse.append(token);
                    emitter.send(SseEmitter.event()
                        .name("message")
                        .data(Map.of("token", token)));
                } catch (IOException e) {
                    throw new RuntimeException(e);
                }
            });
            
            // 6. 发送推荐商品
            if (!products.isEmpty()) {
                emitter.send(SseEmitter.event()
                    .name("products")
                    .data(products));
            }
            
            // 7. 保存助手回复
            conversationService.saveMessage(conversationId,
                MessageRole.ASSISTANT, fullResponse.toString());
            
            // 8. 完成
            emitter.send(SseEmitter.event().name("done").data("[DONE]"));
            emitter.complete();
            
        } catch (Exception e) {
            emitter.completeWithError(e);
        }
    }
}
```

## 5.7 前端实现

### 5.7.1 Axios封装

request.ts封装统一的HTTP请求处理：

```typescript
import axios from 'axios'
import { ElMessage } from 'element-plus'
import router from '@/router'

const request = axios.create({
  baseURL: '/api',
  timeout: 30000
})

// 请求拦截器
request.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  error => Promise.reject(error)
)

// 响应拦截器
request.interceptors.response.use(
  response => {
    const res = response.data
    if (res.code !== 200) {
      ElMessage.error(res.message || '请求失败')
      return Promise.reject(res)
    }
    return res
  },
  error => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      router.push('/login')
      ElMessage.error('登录已过期，请重新登录')
    } else {
      ElMessage.error(error.message || '网络错误')
    }
    return Promise.reject(error)
  }
)

export default request
```

### 5.7.2 SSE客户端实现

assistant.ts实现SSE流式接收：

```typescript
export async function streamChat(
  conversationId: number,
  message: string,
  onToken: (token: string) => void,
  onProducts: (products: Product[]) => void,
  onComplete: () => void
) {
  const token = localStorage.getItem('token')
  
  const response = await fetch('/api/assistant/chat/stream', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${token}`
    },
    body: JSON.stringify({ conversationId, message })
  })
  
  const reader = response.body?.getReader()
  const decoder = new TextDecoder()
  
  while (reader) {
    const { done, value } = await reader.read()
    if (done) break
    
    const chunk = decoder.decode(value)
    const lines = chunk.split('\n')
    
    let eventType = ''
    for (const line of lines) {
      if (line.startsWith('event:')) {
        eventType = line.substring(6).trim()
      } else if (line.startsWith('data:')) {
        const raw = line.substring(5).trim()
        if (!raw) continue
        const data = JSON.parse(raw)
        if (eventType === 'message' && data.token) {
          onToken(data.token)
        } else if (eventType === 'products' && Array.isArray(data)) {
          onProducts(data)
        } else {
          // 处理其他事件或备用消息格式
          if (data.token) {
            onToken(data.token)
          }
        }
        eventType = ''
      }
    }
  }
  
  onComplete()
}
```

### 5.7.3 智能助手页面组件

Assistant.vue实现智能助手界面：

```vue
<template>
  <div class="assistant-container">
    <!-- 会话列表 -->
    <div class="conversation-list">
      <ConversationList 
        :conversations="conversations"
        :activeId="activeConversationId"
        @select="selectConversation"
        @create="createConversation"
      />
    </div>
    
    <!-- 对话区域 -->
    <div class="chat-area">
      <ChatWindow 
        :messages="messages"
        :loading="loading"
      />
      
      <MessageInput 
        v-model="inputMessage"
        :disabled="loading"
        @send="sendMessage"
      />
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { streamChat, getConversations } from '@/api/assistant'

const conversations = ref([])
const activeConversationId = ref<number | null>(null)
const messages = ref([])
const inputMessage = ref('')
const loading = ref(false)

const sendMessage = async () => {
  if (!inputMessage.value.trim() || loading.value) return
  
  const userMessage = inputMessage.value
  inputMessage.value = ''
  
  // 添加用户消息
  messages.value.push({
    role: 'user',
    content: userMessage
  })
  
  // 添加助手消息占位
  const assistantMessage = { role: 'assistant', content: '', products: [] }
  messages.value.push(assistantMessage)
  
  loading.value = true
  
  await streamChat(
    activeConversationId.value!,
    userMessage,
    (token) => {
      assistantMessage.content += token
    },
    (products) => {
      assistantMessage.products = products
    },
    () => {
      loading.value = false
    }
  )
}

onMounted(async () => {
  conversations.value = await getConversations()
})
</script>
```

## 5.8 系统界面展示

### 5.8.1 主要界面说明

**（1）商城首页**
- 顶部导航栏：Logo、搜索框、购物车入口、用户中心
- 商品分类导航
- 商品列表展示区

**（2）商品详情页**
- 商品图片展示
- 价格和库存信息
- 加入购物车和立即购买按钮
- 商品详细描述

**（3）购物车页面**
- 商品列表（图片、名称、单价、数量、小计）
- 数量调整和删除操作
- 总金额计算
- 结算按钮

**（4）智能助手页面**
- 左侧会话列表
- 中间对话区域
- 底部输入框
- 商品推荐卡片

### 5.8.2 日志与异常处理

系统实现过程中，日志与异常处理是保障运行稳定性的关键环节。后端使用Slf4j作为日志框架，在关键业务流程中记录信息级别、警告级别和错误级别日志，例如用户登录、订单创建、LLM调用失败等。前端使用统一的错误提示机制，通过Message组件展示请求失败、网络异常、登录过期等提示。

同时，后端实现了全局异常处理器，统一捕获业务异常、参数校验异常和系统异常，并返回规范化JSON响应。这样不仅提高了系统的一致性，也避免了异常信息泄露。对于智能助手模块，在调用外部LLM服务时，额外捕获API超时、返回格式异常等错误，并向前端返回友好的错误提示。

### 5.8.3 异常处理与日志设计

本系统通过结构化日志记录关键业务事件，确保异常发生时可以快速定位。后端使用Slf4j记录请求入口、业务处理结果、外部服务调用状态和异常堆栈信息，并可与日志聚合平台对接。前端则在请求失败时统一提示用户，如网络错误、认证失败或服务不可用。对于智能助手会话，特别记录生成失败、超时和重试行为，便于后续分析模型调用稳定性和系统表现。

### 5.8.4 工程实践要点

在实际实现中，应保持后端服务与前端页面的功能边界清晰。后端主要负责数据处理、业务校验和外部服务集成；前端主要负责界面展示、用户输入和请求流转。引入自动化测试、代码审查和持续集成流程，可以更早发现缺陷，提升系统质量。

## 5.9 本章小结

本章详细介绍了商城智能购物助手系统的实现过程。首先介绍了开发环境的配置，包括后端Maven依赖、前端Vite配置和数据库初始化。然后按模块详细展示了核心功能的代码实现：用户认证模块实现了JWT生成验证和拦截器；商品模块实现了列表查询和搜索；购物车模块实现了增删改查和库存校验；订单模块实现了订单创建的完整事务流程；智能助手模块实现了RAG检索、LLM调用和SSE流式输出。最后介绍了前端的Axios封装、SSE客户端实现和智能助手页面组件。通过本章的实现，系统完成了从设计到代码的转化，具备了完整的功能。

此外，本章强调了系统实现过程中对开发环境、依赖管理、代码规范和异常处理的重视。这些实现细节为后续系统稳定运行和迭代升级提供了坚实基础，使项目不仅在功能上可用，也具备良好的可维护性。
