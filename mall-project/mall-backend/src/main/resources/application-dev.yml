# 开发环境配置示例

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mall_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 20030420
  
  data:
    redis:
      host: localhost
      port: 6379
      password: 20030420

# 大模型配置（请替换为实际的API密钥）
llm:
  provider: qwen
  qwen:
    api-key: your-qwen-api-key-here
    base-url: https://dashscope.aliyuncs.com/api/v1
