# 商城智能助手系统 - 毕业设计答辩稿

---

## 答辩前准备

### 个人基本信息（请答辩前填写）

| 项目 | 内容 |
|------|------|
| **姓名** | __________________ |
| **学号** | __________________ |
| **专业班级** | __________________ |
| **指导教师** | __________________ |
| **答辩日期** | __________________ |

### 答辩时间控制

| 环节 | 时长 | 内容 |
|------|------|------|
| 自我介绍 | 1分钟 | 姓名、学号、专业、选题 |
| 项目展示 | 10-12分钟 | 系统概述、技术架构、核心功能、创新点 |
| 演示环节 | 5-8分钟 | 功能演示 |
| 答辩问答 | 5-10分钟 | 评委提问、现场回答 |
| 总结致谢 | 1分钟 | 致谢评委 |

---

## 第一部分：开场白与自我介绍（约1分钟）

---

### 开场话术

> 尊敬的各位评委老师，大家好！
>
> 我是来自______专业______班的______，学号______。今天我毕业设计的题目是《基于Spring Boot + Vue3的商城系统与大模型智能助手设计与实现》。接下来，我将向各位老师汇报我的毕业设计工作。

---

## 第二部分：项目概述与研究背景（约2分钟）

---

### 研究背景（可配合PPT第3页）

> **各位老师，在介绍我的系统之前，我先简要说明一下本课题的研究背景。**

近年来，电子商务产业蓬勃发展，以淘宝、京东为代表的电商平台已经深度融入人们的日常生活。然而，传统电商平台在用户体验方面仍存在一些痛点：
>
> **第一**，用户需要明确知道自己想要什么商品才能进行有效搜索，很多用户购物时并没有明确目标，希望通过自然语言描述需求获得推荐；
>
> **第二**，面对海量商品信息，用户往往面临选择困难，容易产生决策疲劳；
>
> **第三**，传统客服系统响应速度慢，无法满足用户即时咨询的需求。

与此同时，以ChatGPT为代表的大语言模型技术取得了突破性进展，RAG（检索增强生成）技术的成熟使得将智能助手融入垂直领域成为可能。**正是基于这样的背景，我设计并实现了商城智能助手系统，将大语言模型技术与电商业务场景相结合。**

---

### 系统定位（配合PPT第3页）

> 我的系统名称是"商城智能助手系统"，它是一个**B/S架构、前后端分离**的电商业务系统。

系统的**差异化核心**是：在真实商城数据之上实现了**对话式智能购物助手**，采用RAG检索增强生成技术，支持商品咨询、订单查询、购物引导等智能化服务。

系统的**建设目标**包括：
- **业务目标**：提供可演示的完整网购闭环与管理闭环
- **智能目标**：助手回答绑定本商城商品与当前用户订单事实，控制幻觉
- **体验目标**：流式打字效果、会话历史、消息下挂可点击商品推荐卡片
- **治理目标**：入站对话先过内容检测，助手可一键暂停或走固定模板

---

## 第三部分：技术架构与技术栈（约2分钟）

---

### 总体架构（配合PPT第4-5页）

> 接下来介绍系统的技术架构。

系统采用经典的三层架构设计：

**表现层**：基于Vue3构建，包含页面路由、助手UI界面、HTTP/SSE消费和状态管理功能。

**应用层**：基于Spring Boot 2.7构建，包含Controller、Service、Mapper三层，实现业务逻辑编排、JWT身份认证、RAG检索和LLM调用等核心功能。

**数据层**：包含MySQL关系型数据库用于业务数据持久化，Redis缓存数据库用于热点数据缓存，以及可选的Chroma或Milvus向量数据库用于语义检索。

---

### 技术栈（配合PPT第6页）

**后端技术栈**：
- 框架：Spring Boot 2.7.18
- ORM：MyBatis-Plus 3.5.3.1
- 数据库：MySQL 8.0 + Druid连接池
- 缓存：Redis
- 安全：JWT令牌认证 + BCrypt密码加密
- AI服务：通义千问API（qwen-turbo模型）

**前端技术栈**：
- 框架：Vue 3 + TypeScript
- 构建：Vite 5
- UI组件：Element Plus
- 状态管理：Pinia
- 流式处理：fetch + ReadableStream

**智能助手技术**：
- RAG检索增强生成
- SSE服务器推送事件
- 可选向量数据库（Chroma/Milvus）

---

## 第四部分：系统功能模块详解（约3分钟）

---

### 用户端功能（配合PPT第7页）

> 系统分为用户端和管理端两大模块。我先介绍用户端功能。

用户端实现了完整的电商业务流程：

| 模块 | 核心功能 |
|------|----------|
| **登录注册** | 用户名密码登录、表单校验、Token存储 |
| **首页** | Hero主视觉、热门商品、最新上架、智能助手入口 |
| **商品列表** | 关键词搜索、分页展示、快速加购 |
| **商品详情** | 商品信息、评价展示、收藏、加购 |
| **购物车** | 数量修改、批量删除、结算功能 |
| **订单管理** | 状态Tab切换、支付、取消订单 |
| **地址管理** | 地址增删改查、设为默认 |
| **个人中心** | 信息修改、密码修改 |
| **收藏** | 收藏列表管理、取消收藏 |
| **智能助手** | 会话管理、消息交互、商品推荐 |

---

### 管理端功能（配合PPT第8页）

> 管理端实现了电商系统的后台管理能力：

| 模块 | 核心功能 |
|------|----------|
| **仪表盘** | 商品/订单/用户统计、最近订单、热销商品 |
| **商品管理** | CRUD、上下架开关、图片上传、AI生成描述 |
| **订单管理** | 筛选、详情查看、发货、取消订单 |
| **订单分析** | 统计数据、图表展示、AI运营分析 |
| **用户管理** | 筛选、状态开关、角色设置 |
| **助手管理** | 设置开关、响应模式、模板管理 |

---

## 第五部分：智能助手核心实现（约4分钟）

---

### 智能助手架构（配合PPT第9-10页）

> 智能助手是本系统的核心亮点。接下来我将详细介绍智能助手的设计与实现。

智能助手的数据流从用户浏览器发起，支持两种传输方式：**JSON方式**适用于非流式响应，**SSE方式**适用于流式响应场景，通过ReadableStream实现打字机效果。

请求首先经过**内容检测拦截器**进行入站内容安全检测，然后到达助手服务进行业务编排。助手服务会调用会话Mapper存储消息，调用设置Mapper获取策略配置，调用RAG服务进行商品和订单检索，最后调用LLM服务生成响应。

---

### 数据模型设计（配合PPT第10页）

> 智能助手涉及四张核心数据表：

| 表名 | 用途 |
|------|------|
| **conversation** | 存储会话主信息，sessionId为UUID格式 |
| **conversation_message** | 存储消息详情，角色1为用户、2为助手 |
| **assistant_settings** | 全局策略配置，控制助手行为 |
| **assistant_template** | 模板回复规则，用于模板模式匹配 |

---

### 完整对话流程（配合PPT第11页）

> 接下来介绍单次对话的后端编排流程，共分为十个步骤：

**第一步：会话解析**。检查请求中是否携带有效的会话ID，如果存在则复用该会话，否则创建新会话并生成新的会话ID。

**第二步：落库用户消息**。将用户消息存储到消息表中，标记为用户角色。

**第三步：管理端策略短路**。读取最新的助手设置，如果助手被停用则直接返回"助手已暂停"；如果是模板模式则遍历模板进行匹配，命中则返回固定回复。

**第四步：RAG检索商品**。调用RAG服务搜索与用户消息相关的商品，默认返回Top 5。

**第五步：RAG检索订单**。根据用户意图判断是否需要检索订单，只有命中订单相关关键词时才执行检索。

**第六步：构建系统提示词**。将商品检索结果、订单检索结果和用户问题组合成完整的提示词。

**第七步：组装多轮历史**。获取该会话最近10条消息作为上下文历史。

**第八步：调用LLM**。根据配置选择非流式或流式调用大语言模型。

**第九步：落库助手消息**。将助手回复存储到消息表中，标记为助手角色，并记录关联的商品ID。

**第十步：返回响应**。将会话ID、回复内容、商品列表等信息返回给前端。

---

### RAG商品检索机制（配合PPT第12页）

> RAG是智能助手准确回答的关键技术。

系统支持两种检索模式：

**向量检索模式**：当配置启用向量检索时，系统首先将用户查询转换为向量嵌入，然后在向量库中进行相似性检索，获取匹配的商品ID列表。

**关键词检索模式**（默认）：通过关键词打分机制实现。打分规则包括：
- 关键词出现在商品名称中，每项加**5分**
- 关键词出现在商品描述中，每项加**3分**
- 关键词出现在商品规格中，每项加**2分**
- 如果查询中包含"便宜"等词，对低价商品额外加分

---

### RAG订单检索机制（配合PPT第13页）

> 订单检索采用严格的安全校验和意图触发机制。

**安全前提**：请求中必须携带用户ID且用户ID大于0，否则直接返回空列表，防止跨用户泄露订单信息。

**意图触发判定**：系统通过多规则判断是否需要检索订单：
- 检测是否包含ORD开头的订单号格式
- 检测是否包含"我的订单"、"订单状态"、"物流"等关键词
- 排除纯流程咨询，如"如何下单"不会触发订单检索

---

### SSE流式输出（配合PPT第14-15页）

> 为了提供流畅的交互体验，系统采用SSE技术实现流式输出。

后端对模型返回的内容进行**语义单元切分**：
- 英文单词攒成完整单词后一次性推送
- 中文按单字符推送
- 每次推送后随机休眠60-100毫秒，形成打字节奏

前端通过fetch API发起请求，使用ReadableStream按行解析响应数据，实现打字机效果。

---

### 管理端策略控制（配合PPT第16-17页）

> 系统支持三种响应模式，由管理端灵活控制：

| 模式 | 行为 | 适用场景 |
|------|------|----------|
| **停用** | 返回固定话术，不调用RAG和LLM | 系统维护、演示 |
| **模板模式** | 遍历模板匹配，命中返回固定回复 | 常见问题快速响应 |
| **智能模式** | 完整执行RAG检索和LLM生成 | 正常业务场景 |

这种设计使得系统既能在高峰期降低成本，又能在紧急情况下快速降级。

---

### 内容安全检测（配合PPT第16页）

> 为了保障系统安全，入站消息会经过内容安全检测。

检测流程：用户消息首先经过内容检测拦截器，调用LLM进行违规检测。如果检测结果显示内容违规，则直接返回错误响应拦截请求；如果合规则继续正常的助手处理流程。

---

## 第六部分：创新点与技术亮点（约1分钟）

---

### 核心创新点（配合PPT第21页）

> 相比同类系统，我的设计具有以下创新点：

**1. 业务数据驱动的对话式导购**
- RAG同时覆盖商品库和用户订单两个数据源
- 提示词强制要求"有证据才答单"，有效控制大语言模型的幻觉问题

**2. 可运营、可降级的助手策略**
- 支持停用、模板、智能三种模式灵活切换
- 模板未命中时自动回退到智能模式

**3. 工程化RAG管道**
- 支持关键词和向量两种检索模式，可配置切换
- 向量检索失败时自动回退到关键词检索

**4. 流畅的用户体验**
- SSE流式响应实现打字机效果
- 语义单元切分确保英文单词完整性和中文流畅性
- 消息下方展示可点击的商品推荐卡片

**5. 安全防护机制**
- 入站消息经过LLM辅助检测，拦截敏感内容
- 订单查询强制校验用户身份，防止数据泄露

---

## 第七部分：功能演示（约5-8分钟）

---

### 演示准备

> 各位老师，接下来我将进行系统的功能演示。

**演示环境说明**：
- 后端服务运行在端口8087
- 前端开发服务器运行在端口3000
- 数据库MySQL运行在端口3306
- 智能助手调用通义千问API

---

### 演示一：用户端基础功能

**演示路径**：登录 → 首页 → 商品列表 → 商品详情 → 购物车 → 下单

**演示话术**：
> 首先演示用户端的电商基础功能。我将展示用户从登录到完成购物的完整流程。
>
> 1. 首先登录系统，使用测试账号登录
> 2. 进入首页，可以看到热门商品推荐和智能助手入口
> 3. 进入商品列表，演示关键词搜索功能
> 4. 点击商品进入详情页，可以看到商品信息、评价、收藏、加购等功能
> 5. 将商品加入购物车
> 6. 进入购物车，可以修改数量、删除商品、批量选择
> 7. 点击结算，选择收货地址，创建订单
> 8. 进入订单列表，可以看到刚创建的订单，可以进行支付或取消操作

---

### 演示二：智能助手功能（重点演示）

**演示路径**：进入智能助手 → 提问商品推荐 → 查询订单 → 展示商品卡片

**演示话术**：
> 接下来演示智能助手功能，这是本系统的核心亮点。

**第一问：商品推荐**
> 我问助手："推荐一款性价比高的键盘"

> 各位老师请注意，后端此时会执行以下操作：
> 1. 先进行内容安全检测
> 2. 调用RAG服务检索商品数据库
> 3. 将检索结果注入提示词
> 4. 调用通义千问生成回复
> 5. 通过SSE流式推送，用户可以看到打字效果
> 6. 回复下方展示关联商品的推荐卡片

**第二问：订单查询**
> 我再问助手："我的订单状态是什么"

> 此时系统会检测到订单相关意图，调用RAG检索当前用户的订单信息，将订单数据注入提示词，助手能够准确回答用户的订单情况。

**演示流式与非流式对比**：
> 我切换到普通模式，可以看到非流式模式下，助手回复是整段返回的，没有打字效果。

---

### 演示三：管理端功能

**演示路径**：管理员登录 → 仪表盘 → 商品管理 → 订单管理 → 智能助手设置

**演示话术**：
> 最后演示管理端功能。
>
> 1. 使用管理员账号登录
> 2. 查看仪表盘，了解系统运营数据
> 3. 进入商品管理，演示添加商品功能
> 4. 进入订单管理，查看订单列表，对订单进行发货操作
> 5. 进入智能助手管理，演示三种响应模式的切换

---

### 演示话术参考

**开场30秒话术**：
> "本项目的智能助手不是孤立的聊天框，后端每次对话都会先查询上架商品与用户订单，将真实业务数据写入提示词后再调用通义千问。因此商品推荐和订单状态都有据可查，有效控制了大语言模型的幻觉问题。"

**RAG演示话术**：
> "刚才提问'有没有便宜的键盘'，系统会执行关键词打分检索，'便宜'这个关键词会对低价商品额外加分，返回Top 5件相关商品。这些商品信息会被写入提示词，助手基于真实商品数据给出推荐。"

**流式演示话术**：
> "请各位老师注意回复的打字效果，后端实现了语义单元切分，英文单词完整推送，中文单字推送，每次推送后有60-100毫秒的随机延迟，形成自然的对话节奏。"

---

## 第八部分：不足与改进方向（约1分钟）

---

### 当前不足（配合PPT第23页）

> 各位老师，任何系统都有改进空间，我的系统也不例外。主要存在以下不足：

| 维度 | 现状 | 问题 |
|------|------|------|
| 用户身份 | 助手路径JWT放行+默认userId | 存在安全隐患，生产环境需强制登录 |
| 物流信息 | 状态文案推导 | 未对接真实快递轨迹API |
| RAG检索 | 商品关键词+订单规则触发 | 检索精度有限 |
| 内容安检 | 每条消息调用LLM检测 | 成本高、延迟大 |

---

### 改进方向

> 针对上述不足，我计划从以下几个方面进行改进：

1. **用户认证加强**：强制JWT验证，删除默认用户，会话按userId强隔离
2. **物流追踪**：对接快递100、菜鸟等轨迹API，提供真实物流信息
3. **检索优化**：引入ES/BM25混合检索、重排序模型，提升检索精度
4. **安检优化**：规则引擎+小模型+抽样审核，降低成本和延迟
5. **向量同步**：异步队列、增量同步，降低同步成本

---

## 第九部分：总结与致谢（约1分钟）

---

### 项目成果总结

> 各位老师，以上就是我的毕业设计汇报。让我总结一下本项目的主要成果：

**1. 完整的电商系统**
- 实现了用户端和管理端的全功能
- 支持商品浏览、购物车、订单、地址、评价、收藏等核心功能

**2. 智能助手核心能力**
- RAG检索增强，同时覆盖商品和订单数据源
- 集成通义千问大语言模型生成响应
- SSE流式响应实现打字机效果
- 内容安全检测拦截敏感内容

**3. 可运营的治理能力**
- 支持多模式切换（停用/模板/智能）
- 会话管理与历史记录

**4. 工程化实现**
- 分层架构清晰，职责分明
- 接口化LLM/RAG设计，易于扩展
- 可配置切换检索模式

---

### 技术价值

> 本系统的技术价值体现在：

- **业务价值**：降低客服成本，提升用户转化率
- **技术价值**：将大语言模型与业务数据深度融合，验证了RAG+LLM在垂直领域应用的可行性
- **架构价值**：采用前后端分离架构，接口化设计，具备良好的扩展性和可维护性

---

### 致谢

> 最后，感谢我的指导老师______老师的悉心指导，从选题确定到技术方案设计，从代码实现到论文撰写，都离不开老师的耐心指点。

> 感谢各位评审老师的宝贵时间和建议！

> 我的汇报到此结束，请各位老师批评指正！

---

## 附录：答辩常见问题准备

### 技术类问题

**Q1：为什么选择RAG而不是直接调用大模型？**
> 直接调用大模型存在知识时效性和准确性问题。RAG技术可以将商品数据库中的最新信息注入提示词，使助手能够基于真实业务数据回答，有效控制幻觉问题。

**Q2：向量检索和关键词检索如何选择？**
> 系统支持两种模式可配置切换。关键词检索无需额外服务，适合快速部署；向量检索语义理解更强，适合复杂查询场景。向量检索失败时会自动回退到关键词检索，保证系统稳定性。

**Q3：SSE和WebSocket有什么区别？为什么选择SSE？**
> SSE是单向通信，实现简单，基于HTTP协议，兼容性好；WebSocket支持双向通信。本系统的智能助手只需要服务端推送数据，SSE完全满足需求，且更易于部署。

**Q4：内容安全检测如何实现？**
> 通过拦截器对入站消息进行检测，调用LLM判断是否包含违规内容。虽然每条消息都会消耗一次LLM调用，但这是保障系统安全的必要代价，后续可通过规则引擎+小模型优化成本。

**Q5：如何保证订单信息不泄露？**
> 订单检索强制要求userId参数，且userId必须大于0。RAG检索时只查询当前登录用户的订单，通过多重校验保证数据安全。

---

### 功能类问题

**Q6：智能助手如何知道用户的订单信息？**
> 用户登录后，系统记录用户身份。当用户询问订单相关问题时，RAG服务会根据userId查询该用户的订单数据，将订单信息注入提示词，助手基于真实数据回答。

**Q7：物流信息是如何获取的？**
> 当前版本是基于订单状态的业务级履约说明，例如"已发货，预计3-5天送达"。真实物流轨迹需要对接快递公司API，这是后续的改进方向。

**Q8：助手回答不准确怎么办？**
> 系统提供了三层保障：1）RAG提供真实商品数据作为证据；2）提示词硬约束要求必须基于检索结果回答；3）管理端可以设置模板回复覆盖特定问题。

---

### 扩展类问题

**Q9：系统的性能如何？**
> 后端采用Spring Boot + MyBatis-Plus，数据库设计了合理索引；前端采用Vue3 + Vite，按需加载组件。SSE流式响应降低了用户感知的等待时间。

**Q10：系统如何部署？**
> 后端打包为jar文件，前端构建为静态资源。可以通过Docker容器化部署，也支持传统方式部署到服务器。配置文件中需要注意数据库密码、API密钥等敏感信息的脱敏处理。

**Q11：系统如何扩展？**
> 系统采用接口化设计，LLM服务和RAG数据源都是抽象接口。如果需要更换大模型提供商，只需实现新的服务类；如果需要检索更多数据源，只需实现新的RAGDataSource。这种设计保证了系统良好的可扩展性。

---

## 附录：演示故障排除

### 常见问题应对

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 前端无法加载 | 后端未启动 | 检查后端服务是否运行在8087端口 |
| 智能助手无响应 | LLM API配置错误 | 检查API密钥和网络连接 |
| 流式响应卡顿 | 网络延迟 | 切换到普通模式演示 |
| 商品图片不显示 | 图片路径错误 | 检查静态资源路径配置 |

### 备用演示方案

如果现场网络不稳定导致LLM调用失败：
1. 提前准备截图展示正常效果
2. 演示管理端模板模式（不调用LLM）
3. 说明原因，表示会在后续优化网络配置

---

## 附录：PPT制作建议

### 每页PPT内容控制

- 每页PPT要点控制在3-6个
- 避免大段文字，使用关键词和短语
- 配合图表和流程图增强表达
- 深色背景配浅色文字，便于投影显示

### 建议的PPT页面结构

1. **封面**：标题 + 个人基本信息
2. **目录**：汇报大纲
3. **项目概述**：背景 + 系统定位 + 建设目标
4. **技术架构**：三层架构图 + 技术栈表格
5. **用户端功能**：功能模块表格
6. **管理端功能**：功能模块表格
7. **智能助手-架构**：数据流框图
8. **智能助手-数据模型**：ER图/表格
9. **智能助手-对话流程**：时序图
10. **智能助手-RAG检索**：流程图
11. **智能助手-流式输出**：示意图
12. **智能助手-策略控制**：对比表格
13. **创新点**：列表形式
14. **演示**：过渡页，标注"现场演示"
15. **不足与改进**：表格形式
16. **总结**：要点列表
17. **致谢**：简洁页面
18. **问答**：过渡页

---

**文档版本**：答辩稿完整版
**生成日期**：2026年5月
**基于项目**：mall-project 商城智能助手系统
