# 商城智能助手系统 — 毕业答辩讲稿

---

## 讲稿结构与时间分配（总计15分钟）

| 环节 | 时间 | 主要内容 |
|------|------|----------|
| 开场介绍 | 1分钟 | 自我介绍、项目定位 |
| 研究背景与意义 | 2分钟 | 电商发展现状、AI技术突破、研究价值 |
| 系统架构设计 | 2分钟 | 前后端分离架构、逻辑分层、技术栈 |
| **智能助手核心实现** | **4分钟** | **RAG技术、LLM集成、流式响应** |
| 功能模块演示 | 3分钟 | 用户端、管理端核心功能 |
| 创新点与技术亮点 | 1分钟 | 技术创新、工程实践 |
| 总结与展望 | 1分钟 | 成果总结、未来改进方向 |
| 致谢与提问 | 1分钟 | 致谢、准备答疑 |

---

## 一、开场介绍（1分钟）

尊敬的各位老师、同学们：

大家好！我是[您的姓名]，来自[专业名称]，我的指导老师是[导师姓名]教授。

今天我将向大家汇报我的毕业设计项目——**《商城智能助手系统》**。

本系统是一个基于 **Spring Boot + Vue3** 的电商平台，核心特色是集成了 **RAG（检索增强生成）+ 大语言模型** 的智能购物助手，实现了对话式商品推荐、订单查询等智能化功能。

接下来，我将从研究背景、系统架构、核心技术实现等方面进行详细阐述。

---

## 二、研究背景与意义（2分钟）

### 2.1 电子商务发展现状

随着互联网技术的快速发展，中国网络购物用户规模已超过9亿人。但传统电商平台存在以下痛点：

- **搜索局限性**：用户需明确知道商品名称才能有效搜索
- **信息过载**：面对海量商品难以做出选择，产生决策疲劳
- **客服响应慢**：无法满足用户即时咨询需求

### 2.2 人工智能技术突破

以GPT、通义千问为代表的大语言模型展现出强大的自然语言理解和生成能力。但单纯的LLM存在以下问题：

- **知识时效性不足**：模型训练数据有时间窗口
- **领域专业性欠缺**：缺乏特定业务领域知识
- **容易产生幻觉**：可能生成虚假信息

### 2.3 RAG技术的解决方案

检索增强生成（RAG）技术通过将外部知识库与LLM相结合，使模型能够基于最新的、特定领域的知识进行回答，有效解决了上述问题。

### 2.4 研究意义

本研究的意义在于：
- **理论意义**：探索RAG技术在电商场景的应用方法
- **实践意义**：提供可参考的智能购物助手系统实现方案
- **教育意义**：涵盖前后端开发、数据库设计、AI集成等全栈技术

---

## 三、系统架构设计（2分钟）

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        表现层 (Vue3)                            │
│   用户端：首页、商品、购物车、订单、智能助手                      │
│   管理端：仪表盘、商品管理、订单管理、用户管理、助手配置          │
├─────────────────────────────────────────────────────────────────┤
│                        应用层 (Spring Boot)                     │
│   Controller → Service → Mapper                                │
│   JWT认证、RAG检索、LLM调用、SSE流式输出                        │
├─────────────────────────────────────────────────────────────────┤
│                        数据层                                   │
│   MySQL + MyBatis-Plus：业务数据持久化                          │
│   Redis：缓存                                                  │
│   可选向量库 (Chroma/Milvus)：语义检索                          │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 技术栈

| 分类 | 技术 | 版本 |
|------|------|------|
| 后端框架 | Spring Boot | 2.7.18 |
| ORM框架 | MyBatis-Plus | 3.5.3.1 |
| 数据库 | MySQL | 8.0+ |
| 缓存 | Redis | 6.0+ |
| 前端框架 | Vue | 3.3 |
| UI组件库 | Element Plus | 2.4 |
| 构建工具 | Vite | 5.x |
| 大语言模型 | 通义千问 | qwen-turbo |

### 3.3 核心数据模型

| 表名 | 用途 | 核心字段 |
|------|------|----------|
| conversation | 会话管理 | userId, sessionId, title |
| conversation_message | 消息记录 | conversationId, role, content, relatedProducts |
| assistant_settings | 助手配置 | name, enabled, responseMode, timeout |
| assistant_template | 回复模板 | keyword, response |

---

## 四、智能助手核心实现（4分钟）【重点环节】

### 4.1 单次对话编排流程

```
用户提问 → 会话解析 → 落库用户消息 → 管理端策略短路 → RAG商品检索 → RAG订单检索 
    → 构建系统提示词 → 组装多轮历史 → 调用LLM → 落库助手消息 → 返回响应
```

### 4.2 RAG检索技术

#### 4.2.1 商品检索（关键词打分策略）

```java
// 打分规则
- 关键词在商品名中出现：+5分
- 关键词在描述中出现：+3分  
- 关键词在规格中出现：+2分
- 查询含"便宜""入门"：低价商品额外加分
- 查询含"高端""旗舰"：高价商品额外加分
```

**优势**：无需向量库也能实现精准检索，降低部署门槛。

#### 4.2.2 订单检索（意图触发机制）

```java
// 触发条件（满足任一）
- 正则匹配订单号：\b(ORD\d+)\b
- 包含关键词："我的订单""订单状态""物流""快递"等
- 排除纯流程咨询："如何下单""下单流程"等
```

**安全机制**：必须携带userId，确保不跨用户泄露订单隐私。

### 4.3 提示词工程

系统提示词结构：

```
你是商城智能购物助手，仅回答与本商城购物相关的问题。

【订单系统检索结果】（当前登录用户，来自数据库）
- 订单号：ORD202401010001，金额：199.00元，状态：已发货
- 订单号：ORD202401020002，金额：299.00元，状态：待支付

【商品检索结果】
1. 机械键盘 Pro版 - 199元 - RGB背光，87键热插拔
2. 无线鼠标 Elite - 99元 - 蓝牙5.0，超长续航

请基于以上信息回答用户问题，务必引用商品名称和价格。
```

**防幻觉设计**：
- 订单信息必须严格依据检索结果
- 商品推荐必须来自商品库
- 无数据时引导用户到对应页面

### 4.4 大语言模型集成

采用**通义千问 qwen-turbo**模型，支持：
- **普通模式**：完整响应返回
- **流式模式**：SSE实时推送

#### 流式响应实现

**后端**（SSE封装）：
```java
SseEmitter emitter = new SseEmitter(60000L);
// 设置防缓冲响应头
emitter.send(SseEmitter.event()
    .name("message")
    .data(chunk));
```

**前端**（ReadableStream解析）：
```javascript
const reader = response.body.getReader();
const decoder = new TextDecoder('utf-8');
while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    const chunk = decoder.decode(value);
    // 处理流式数据
}
```

**语义单元切分**：
- 连续英文单词完整推送
- 中文按单字符推送
- 随机延迟60-100ms，模拟打字效果

### 4.5 管理端策略控制

| 模式 | 说明 | 应用场景 |
|------|------|----------|
| intelligent | 智能模式（默认） | 正常业务运行 |
| template | 模板模式 | 关键词命中返回固定回复 |
| disabled | 停用模式 | 系统维护、成本控制 |

---

## 五、功能模块演示（3分钟）

### 5.1 用户端功能

| 模块 | 功能描述 | 核心接口 |
|------|----------|----------|
| 登录注册 | JWT认证、密码加密 | POST /user/login |
| 商品浏览 | 列表展示、搜索筛选、详情查看 | GET /products |
| 购物车 | 添加商品、修改数量、结算下单 | POST /cart/add |
| 订单管理 | 订单列表、支付取消、状态查看 | POST /order/create |
| 智能助手 | 对话咨询、商品推荐、订单查询 | POST /assistant/chat/stream |

### 5.2 管理端功能

| 模块 | 功能描述 | 核心接口 |
|------|----------|----------|
| 仪表盘 | 数据统计、指标展示 | GET /admin/dashboard |
| 商品管理 | CRUD、上架开关、AI生成描述 | POST /products |
| 订单管理 | 订单列表、发货处理、数据分析 | GET /admin/orders |
| 用户管理 | 用户列表、角色管理、状态控制 | GET /admin/users |
| 助手配置 | 启用开关、响应模式、模板管理 | PUT /admin/assistant/settings |

### 5.3 演示要点

1. **智能助手对话**：展示自然语言交互、流式响应效果
2. **RAG商品检索**：提问"推荐便宜的键盘"，展示关键词打分效果
3. **订单查询**：提问"我的订单状态"，展示订单RAG检索
4. **管理端配置**：演示模板模式、停用模式的策略控制
5. **内容安全检测**：演示敏感词拦截功能

---

## 六、创新点与技术亮点（1分钟）

### 6.1 技术创新

1. **业务数据驱动的对话式导购**：RAG同时覆盖商品库与用户订单，提示词强制"有证据才答"
2. **可运营、可降级**：支持停用/模板/智能三态切换，贴合真实运维需求
3. **工程化RAG管道**：关键词与向量双模式、失败自动回退、商品变更upsert/delete钩子
4. **体验优化**：SSE流式输出、语义单元推送、前端乐观更新

### 6.2 工程实践亮点

- **接口抽象**：LLMService、RAGService接口化设计，支持多厂商切换
- **分层架构**：Controller→Service→Mapper清晰分层
- **异常处理**：全局异常处理器、LLM超时降级
- **安全认证**：JWT无状态认证、密码BCrypt加密

---

## 七、总结与展望（1分钟）

### 7.1 成果总结

本系统实现了：
- 完整的电商业务闭环（用户端+管理端）
- 基于RAG+LLM的智能购物助手
- SSE流式响应的流畅交互体验
- 可运营、可降级的助手管理策略

### 7.2 未来改进方向

| 改进点 | 现状 | 改进方案 |
|--------|------|----------|
| 用户身份校验 | 助手路径JWT放行 | 强制JWT验证 |
| 物流信息 | 状态文案推导 | 对接快递轨迹API |
| RAG检索 | 关键词为主 | 引入ES/BM25混合检索 |
| 内容安检 | 每条消息调LLM | 规则引擎+小模型 |
| 向量同步 | 全量同步 | 异步队列、增量同步 |

---

## 八、致谢与提问（1分钟）

尊敬的各位老师，以上就是我的论文汇报内容。

在此，我要特别感谢我的指导老师[导师姓名]教授，在整个研究过程中给予我的悉心指导和无私帮助。同时，也要感谢实验室的同学们在项目开发过程中提供的支持与帮助。

欢迎各位老师批评指正！

---

## 附录：预期答辩问题准备

| 问题类型 | 可能问题 | 回答要点 |
|----------|----------|----------|
| 技术细节 | RAG检索的具体实现原理？ | 关键词打分规则、订单意图触发机制 |
| 方法创新 | 为什么采用关键词+向量双模式？ | 降低部署门槛、保证检索准确性 |
| 系统设计 | 如何保证订单数据安全？ | userId强制校验、不跨用户查询 |
| 性能优化 | 流式响应如何实现？ | SSE+语义单元切分+打字延迟 |
| 实际应用 | 如何处理LLM调用失败？ | 降级到模板模式、返回友好错误提示 |
| 未来展望 | 下一步的优化方向是什么？ | 物流API对接、ES混合检索、增量同步 |

---

**讲稿结束**

> 注意事项：
> 1. 实际汇报时注意控制时间，重点演示智能助手功能
> 2. PPT配合讲稿使用，重点突出架构图和流程图
> 3. 提前准备演示环境，确保演示流畅
> 4. 保持语速适中，与听众保持眼神交流